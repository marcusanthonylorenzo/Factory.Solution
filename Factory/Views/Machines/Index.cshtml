@{
  Layout = "_Layout";
}

@using Factory.Models;

<section class="details-maininfo">

    <section class="details-maininfo-card">

        <h1>Machine Database</h1>

        @if (@Model.Count == 0)
        {
          <h3>No machines available at this time.</h3>
        } 

        <ul>
          @foreach(Machine machine in Model)
          {
            <li>@Html.ActionLink($"{machine.Name}", "Details", new { id = machine.MachineId })</li>
          }
        </ul>

        <p>@Html.ActionLink("Add new machine", "Create")</p>
        <p>@Html.ActionLink("Home", "Index", "Home")</p>

    </section>
</section>